# Changelog

## 0.1.0 - 2026-05-11

- Prepare the first PyPI release.
- Declare the direct `httpx` runtime dependency used by remote session support.
- Limit default pytest discovery to package tests; bundled skill integration
  tests still require an explicit OpenFOAM `sim-server`.
